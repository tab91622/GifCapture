在这里放置 `icon.icns`，用于 macOS `.app/.dmg` 打包显示应用图标。

你可以先准备一个 1024x1024 的 PNG，然后转换为 `.icns`。
在这里放置 `icon.icns`，用于 macOS `.app/.dmg` 打包时显示应用图标。

可以先准备一个 1024x1024 PNG，再转换为 `.icns`。
在这里放置 `icon.icns`，用于 macOS `.app/.dmg` 打包显示应用图标。

你可以先准备一个 1024x1024 的 PNG，然后转换为 `.icns`。
在这里放置 `icon.icns`，用于 macOS `.app/.dmg` 打包显示应用图标。

你可以先准备一个 1024x1024 的 PNG，然后转换为 `.icns`。
